# 01. 매입처발주.py — 1단계: 발주총괄 → 구글시트 업로드

문서 버전: v1.9.1 (코드 버전과 동일하게 추적)
최종 수정일: 2026-06-30
이전 MD 기준 버전: v1.6 (2026-06-22 작성 후 갱신 누락 — 이번에 한 번에 따라잡음)

---

## 0. 입력/출력/의존관계

```text
입력 파일:
  Z:\★풍림전자 사무 자동화\매입처 관리\1단계_발주원장구글시트업로드\원본\
    발주총괄_YYMMDD.xlsx  (사방넷 다운로드, 매일 1개)

참조 파일 (읽기 전용):
  Z:\★풍림전자 사무 자동화\매입처 관리\1단계_발주원장구글시트업로드\
    캐리어_모델단가테이블.xlsx   (모델코드, 채널, 공급가_설치포함, 공급가_설치제외)
    윈세이_모델단가테이블.xlsx
    스타리온_모델단가테이블.xlsx

출력:
  Google Sheets "풍림_매입처 관리시트"
  (https://docs.google.com/spreadsheets/d/17VFCCy8UTr_UyWhnw1F1n0ZivY6G1XMeOv7VnkuQeRY)
    캐리어_발주원장 / 윈세이_발주원장 / 스타리온_발주원장
  발주총괄 원본 파일 → 완료\ 폴더로 이동

인증키:
  Z:\★풍림전자 사무 자동화\사무실 입고 및 AS\poonglim-0f980142b793.json

이전 단계: 없음 (사방넷에서 발주총괄 수동 다운로드)
다음 단계: 02_매입처발주서생성.md (2단계, 발주서생성.py)

같은 폴더의 1회성 보조 스크립트:
  캐리어발주원장_마감단가백필.py — 정기 실행 아님, 아래 6번 항목 참조
```

---

## 1. 역할

사방넷에서 다운로드한 발주총괄 엑셀 1개를 매입처별로 자동 분기해서 구글시트 발주원장 3개(캐리어/윈세이/스타리온)에 업로드한다. 캐리어 행은 동시에 매입정산용 마감단가(설치비포함 기준)까지 계산해서 같이 적재한다.

매입처 필터 기준 (발주총괄 엑셀 값 — 사방넷 화면 표시명과 다름):

| 발주총괄 엑셀 값 | 실제 매입처 |
|---|---|
| 오텍캐리어(주) | 캐리어 |
| kyungginambupoonglim | 윈세이 |
| 쿠쿠부천점 | 스타리온 |

---

## 2. 캐리어_발주원장 컬럼 (24개, v1.9부터)

```text
접수일, 구분, 완료여부, 오더번호,
모델코드, 수량, 설치점구분, 설치점코드, 배송구분,
캐리어공급가_VAT제외, 발주금액_VAT포함, 납기요구일,
주문자명, 실구매자명, 실구매자휴대폰, 실구매자전화번호,
실구매자우편번호, 실구매자주소,
물류메시지, 사방넷주문번호, 채널, 설치점주소,
CS여부, CS내용,
설치비_VAT제외, 마감단가_VAT제외, 마감금액_VAT제외, 마감금액_VAT포함   ← v1.9 신규
```

`get_worksheet()`가 시트에 이미 헤더가 있어도, 코드에 정의된 컬럼 중 빠진 게 있으면 기존 데이터를 보존한 채 맨 뒤에 자동으로 헤더를 추가한다 (캐리어단가이력업로드.py v1.2의 '채널' 컬럼 자동추가와 동일 패턴).

---

## 3. 마감단가 4종 — 매입정산 vs 발주서 단가의 구분 (v1.9 핵심)

### 배경

캐리어 전산은 물품대(설치비제외, 월별 변동)와 설치비(연간 고정)를 분리해서 관리한다. 발주서를 만들 때는 설치점이라도 Z01(설치비포함)/Z02(설치비미포함) 분기로 캐리어가 알아서 처리하므로, **발주서 자체는 의도적으로 설치비제외 단가(`캐리어공급가_VAT제외`)를 쓴다 — 이건 맞는 동작이고 바뀌지 않았다.**

문제는 매입정산(캐리어와의 마감 대조)이었다. 설치 건은 실제로는 설치비까지 포함된 금액으로 마감되어야 하는데, 이 흐름이 구글시트에 전혀 없었다. (2026-06-30 Dr. Corcan 보고로 발견)

### 계산 로직

```text
모델코드에 '-설치비제외' 접미사 있음 (사방넷 발주총괄 원본에서부터 붙어옴)
  → 설치점 배송이어도 무조건 배송상품(설치비 없음) 취급

모델코드에 '-할인점' 접미사 있음 (사방넷 발주총괄 원본에서부터 붙어옴)
  → 설치비와 무관, 채널만 무조건 벤더공통으로 강제 (홈쇼핑 전용 모델 판정 무시)

단가 매칭 시 위 두 접미사는 모두 떼고 모델코드_단가조회용으로 조회한다.

설치건여부 = (배송구분 == '설치점') AND (설치비제외 접미사 없음)

마감단가_VAT제외 = 설치건여부 ? 공급가_설치포함 : 공급가_설치제외
설치비_VAT제외   = 공급가_설치포함 - 공급가_설치제외   (검증용, 모델별 고정값)
마감금액_VAT제외 = round(마감단가_VAT제외 × 수량)
마감금액_VAT포함 = round(마감단가_VAT제외 × 수량 × 1.1)   ← 합산 후 마지막에 한 번만 ×1.1
```

물품대와 설치비를 각각 따로 VAT 붙여서 더하지 않는다 — 반드시 합산 먼저, VAT는 마지막에 한 번만.

### 부수 효과 — 버그 해소

접미사(`-설치비제외`, `-할인점`)가 붙은 모델코드는 v1.8까지 단가표 매칭에 실패해서 `캐리어공급가_VAT제외`가 빈칸으로 들어가고 있었다. v1.9.1에서 접미사를 떼고 매칭하도록 고치면서 이 문제도 함께 해소됨.

---

## 4. 단가표 로딩

```python
carrier_price       = load_price_table(..., '공급가_설치제외', '채널')  # 발주서용
carrier_price_포함  = load_price_table(..., '공급가_설치포함', '채널')  # 마감정산용 (v1.9 신규)
```

`캐리어_모델단가테이블.xlsx`는 매달 `캐리어단가이력업로드.py`(0단계)가 운영안 파일을 읽어 `공급가_설치포함`/`공급가_설치제외` 둘 다 자동 갱신한다 — 0단계는 이미 정상 동작 중이라 1단계 작업으로 추가 수정한 것은 없음.

---

## 5. 변경 이력 (v1.6 → v1.9.1, 한 번에 정리)

```text
v1.7 (2026-06-22)
  - 스타리온 해피콜대기 → 출고대기 전환 시 '발주대기' 자동 갱신 추가
  - 캐리어 중복 카운터 이중 증가 버그 수정
  - 기존 완료여부 조회를 매 행 get_all_records() 대신 실행 시작 시 1회 캐싱으로 변경

v1.8 (2026-06-22)
  - 전화번호 포맷 보정(format_phone) 추가 — 앞자리 0 누락 복원
  - 우편번호 5자리 미만 시 0 채움 보정

v1.9 (2026-06-25)
  - 캐리어_발주원장에 마감단가 4개 컬럼 신규 추가 (위 3번 항목)
  - 공급가_설치포함 단가도 함께 로드
  - '-설치비제외' 접미사 인식 (위 3번 항목)
  - get_worksheet() 누락 컬럼 자동 헤더 추가 보강
  - 과거 누적 데이터는 별도 1회성 백필 스크립트로 처리하기로 결정

v1.9.1 (2026-06-30)
  - '-할인점' 접미사 인식 추가 (채널을 벤더공통으로 강제) — 마감단가 백필 1차 실행에서
    ESQC231AAWWSD-할인점 매칭실패 사례로 발견
```

---

## 6. 캐리어발주원장_마감단가백필.py — 1회성 보조 스크립트

```text
목적: v1.9 적용 이전에 이미 시트에 쌓여있던 과거 행들의 마감단가 4개 컬럼을
      1회성으로 채워 넣는다. 정기 실행 스크립트가 아니다.

전제: 캐리어 거래는 2026년 6월부터 시작했고, 캐리어_모델단가테이블.xlsx은
      6월 단가만 보유. 그래서 월별 단가 역추적 없이 현재 단가표를 그대로
      과거 행 전체에 적용해도 정확함. (7월 이후 추가 백필이 필요해지면
      이 전제가 깨지므로 캐리어_단가이력의 연월별 단가로 재설계해야 함)

동작: 마감단가_VAT제외가 비어있는 행만 대상으로 함 (이미 채워진 행은
      건드리지 않음 — 여러 번 실행해도 안전). batch_update로 일괄 셀 업데이트.

2026-06-30 1차 실행 결과: 45건 중 38건 성공, 7건(고유 모델 2종) 실패
  - EARB-0061FAWSD: 구형 전시재고 모델, 단가표에 없는 게 정상 (매입정산 대상 아님)
  - ESQC231AAWWSD-할인점: '-할인점' 접미사 미인식이 원인 → v1.1로 코드 수정 후
    재실행해 해결 (1건 성공, EARB-0061FAWSD 6건만 남음 — 정상)
```

---

## 7. suppliers.html 현황판 — 마감금액 카드 (2026-06-30, 같은 날 1차 수정 후 2차 수정 있음)

### 1차 수정 (당일 오전)

`ops.corcan.co.kr/operations/suppliers.html`의 "💰 이번달 발주 현황" 단 아래에 신규 섹션 추가:

```text
🧾 이번달 매입정산 마감금액 — 설치비포함, 캐리어 마감 대조용
  📦 이번달 마감건수
  💰 이번달 마감금액 (VAT별도 / VAT포함 두 줄 동시 표시)
```

계산은 화면(JS)에서 `allOrders`에 이미 포함된 `마감금액_VAT제외`/`마감금액_VAT포함`을 직접 합산하는 방식 — 서버 Apps Script(`getCarrierSummary`)는 건드리지 않음 (서버 코드 백업이 없어 안전하게 우회).

### 2차 수정 (당일 오후, Dr. Corcan 실사용 피드백 반영)

실제 화면을 본 뒤 세 가지 문제가 발견되어 수정함.

**문제 1 — 발주현황 목록(탭2) 금액이 여전히 설치비제외**
목록 행의 금액 컬럼이 `발주금액_VAT포함`(발주서 기준)을 그대로 쓰고 있었음. → **`마감금액_VAT포함`(매입정산 기준)으로 변경.** 마감금액이 없는 행(백필 전이거나 단가매칭 실패)만 "(발주서기준)" 표시를 달아 발주금액을 대신 표시.

**문제 2 — "이번달 발주금액" 카드가 오히려 혼란**
설치비제외 금액인데 "발주금액"이라고만 표시돼 있어 마감금액 카드와 헷갈림. → **카드 자체를 제거.** 3단은 "이번달 발주건수" / "전체 주문건수" 2개 카드로 단순화.

**문제 3 — 발주건수(37) vs 마감건수(40) 불일치**
1차 수정 때 마감건수/마감금액 집계를 `완료여부 IN (발주완료, 기존재고)`로 잡았던 게 원인. **'기존재고'는 이번 달에 캐리어로부터 새로 청구되는 금액이 아니다** — 예전에 매입해서 설치점에 미리 쌓아둔 재고를 꺼내 쓰는 것뿐이고, 그 매입대금은 처음 매입한 시점에 이미 정산이 끝난 상태. → **마감건수/마감금액 집계 기준을 '발주완료'만으로 축소.** 이후 마감건수(37) = 발주건수(37)로 정확히 일치.

```text
집계 기준 최종본:
  접수일이 이번달 AND 완료여부 = '발주완료'   ← 기존재고 제외 확정
```

기존 "이번달 발주금액" 카드(설치비제외, 발주서 기준)는 완전히 제거됨 — 위 문서 초판(1차 수정 시점) 설명과 달라진 부분이므로 혼동 주의.

발주현황 탭 개별 건 펼침 화면에도, 설치 건이면 마감금액(VAT+) 한 줄이 추가로 표시됨 (변동 없음).

---

## 8. 할 일 (이 문서 기준)

```text
- [ ] 00_캐리어운영안.md → 실제 코드(캐리어단가이력업로드.py v1.2)와 일치 여부
      재확인 (이번 작업 범위 아님, 별도 진행 필요)
- [ ] 02_매입처발주서생성.md에 마감단가 관련 영향 없음을 명시할지 검토
      (2단계는 이번 변경과 무관 — 마감단가는 1단계에서 끝남)
- [ ] 7월 데이터가 쌓이면 "이번달 마감금액"(발주완료 기준) 합계를 실제 캐리어
      마감서와 대조해 로직 검증
- [ ] README.md 문서 목록 표의 01번 버전 표기를 v1.6 → v1.9.1로 갱신
```
