# 매입처 발주 자동화 문서 세트

이 문서 세트는 풍림전자 에어컨/제습기 매입처(캐리어·윈세이·스타리온) 발주 자동화 업무를
AI 에이전트와 안전하게 협업하기 위한 기준 문서다.

원칙: 각 문서는 해당 단계 프로그램의 **현재 동작 기준**만 담는다. 과거 버전 이력은 보관하지
않는다. 코드를 수정하면 그 즉시 해당 문서도 함께 수정한다.

## 문서 목록

> 🔄 **2026-07-08 격차 재점검 (3차 — 00~07 전 범위 실제 소스 대조 완료)**: 1차
> 재점검(Private 저장소 미접근, 프로젝트 지식 v28에만 의존) → 2차(6개 .py
> 소스 직접 대조) → 3차(05_윈세이스타리온_매입마감.md 초안 + Code.gs v13.2
> 소스까지 추가로 받아 대조)까지 이어짐. 이제 00~07 전 범위가 실제 소스
> 기준으로 확정됐다. 이 과정에서 v28의 오기 1건(아래 03번), 05번 문서가
> 옛날 매칭 방식을 설명하고 있던 것(아래 콜아웃) 등을 발견함.

| 파일 | 역할 | MD 기준 버전 | 실제 코드 버전 (.py 소스 직접 확인, 2026-07-08) | GitHub 마지막 백업 | 상태 |
|---|---|---|---|---|---|
| `00_캐리어운영안.md` | 0단계 — 캐리어단가이력업로드.py. 캐리어 운영안(매달 수신) → 단가테이블/이력 관리 **+ [v1.5신규] VRF이마트 단가도 같은 파일에서 함께 추출** | v1.1 | **v1.5** (v28 문서관리표의 "v1.4"는 틀렸고, 미백업 코드목록의 "v1.5"가 맞았음 — 소스로 확정) | v1.1 수준 문서만 있음 | ⚠️ MD 갱신 필요 — VRF 처리 자체가 MD에 전혀 없음 |
| `01_매입처발주.md` | 1단계 — 매입처발주.py. 발주총괄 → 캐리어/윈세이/스타리온 구글시트 분기 업로드 | v1.6 → **v1.9.2로 정정** (문서 본문 자체 선언, 06-30 커밋 — 표만 안 고쳐져 있었음) | **v1.10.1** (docstring 제목은 아직 "v1.10"만 표기 — 아래 배너 지연 항목 참조) | v1.9.2 (문서 본문 기준) | ⚠️ MD 갱신 필요 — v1.9.2(문서)~v1.10.1(코드) 격차 여전 |
| `02_매입처발주서생성.md` | 2단계 — 발주서생성.py. 구글시트 → 매입처별 발주서 생성 + 발송 | v2.5 | **v2.7** (소스 확인) | v2.6 | ⚠️ MD 갱신 필요 — v2.6→v2.7 백업도 아직 안 됨 |
| `03_발주원장업데이트.md` | 3단계 — 캐리어오더번호업데이트.py(**v1.4**, 묶음오더/접미사매칭/중복감지) / 윈세이송장업데이트.py(**v2.0**) | 작성 완료 | 캐리어 **v1.4** / 윈세이 **v2.0** (소스 확인 — ⚠️ v28에 적혀 있던 "윈세이 v2.2"는 오기. 소스 docstring·콘솔배너 모두 v2.0이고, v2.1/v2.2가 존재했다는 흔적이 코드 어디에도 없음) | 캐리어 v1.3 / 윈세이 v1.1 | ⚠️ MD 갱신 필요 — 캐리어 묶음오더 로직(v1.4) 전체 미반영 |
| `03b_캐리어_발주원장_검증.py` (별도 MD 없음) | 1→2단계 사이 읽기 전용 정합성 검증기. 처리키 중복/형식, 완료여부 정합성, 재고현황 건전성, 원본 대비 분할합계 확인 | — | **v0.1** (소스 확인) | 없음 | 🔴 MD 자체가 없음 — `07_발주자동화워처.md`나 `03_발주원장업데이트.md`에 편입 필요. 스크립트 자신의 docstring엔 "다음 단계: 워처에 연결"이라 적혀 있지만, 실제로는 이미 `풍림_발주자동화_워처.py`의 체인A에 연결되어 있음(`VALIDATOR_SCRIPT` 참조 + `run_validator()` 호출 확인) — 스크립트 자체 상태 기록이 뒤처짐 |
| `04_에브리데이발주.md` | 1단계(별도) — 에브리데이발주.py(v1.4). 이마트 에브리데이 VRF 발주요청서 → 캐리어_발주원장 | **v2.0** (07-08 작성 완료, 전체 파이프라인 실사용 검증 완료) | 에브리데이 **v1.4** / 발주서생성 **v2.7** / 3단계 **v1.4** (모두 소스 확인) | v2.0 (07-08 백업됨) | ✅ 문서는 최신. 단, 관련 .py 3종은 GitHub에 아직 없음 |
| `05_윈세이스타리온_매입마감.md` | 발주 이후 단계 — 윈세이/스타리온 1차 매입마감 (suppliers.html + Code.gs) | 2026-06-25 작성, "Apps Script v12" 명시 | Code.gs **v13.2** (소스 확인). 단, v12→v13.2 변경분(v13/v13.1/v13.2)은 전부 **캐리어 부분재고분할 관련 수정**이라 05 문서가 다루는 윈세이/스타리온 함수(updateWinseiTracking/cancelWinseiOrder/markWinseiReturn/requestWinseiReturn/completeWinseiReturn/confirmStarionInstallation/cancelStarionOrder) 자체는 코드로 대조해도 변경 없음 확인됨 | 2026-06-25 수준 | ⚠️ MD 갱신 필요 — 아래 "🆕 05번 문서의 더 큰 문제" 참조 (버전 표기보다 심각) |
| `06_풍림_매입처관리_AppsScript.md` | Google Apps Script(Code.gs) — suppliers.html의 서버 로직(updateOrderNo/cancelCarrierOrder/deleteCarrierOrder 등) | — | **v13.2** (소스 확인). v12→v13: 기존재고 건 오더번호 입력 시 완료여부를 발주완료로 잘못 덮어쓰던 버그 수정(김정미님 EARB-0061FAWSD 실사례). v13→v13.1: 이번달 주문건수 집계를 원장 행 기준→사방넷주문번호 고유개수 기준으로 변경(부분재고분할로 한 주문이 2행이 되는 것에 대응). v13.1→v13.2: updateOrderNo/cancelCarrierOrder/deleteCarrierOrder 세 함수 모두, 같은 사방넷주문번호에 처리행이 2개 이상이면(부분재고분할) 처리구분으로 좁히고 그래도 안 되면 추측하지 않고 에러로 멈추도록 안전장치 추가 | 없음 | 🔴 작성 예정 (문서 아직 없음, 소스는 이제 확보됨) |
| `07_발주자동화워처.md` | 1→2단계 자동 체인(1단계→검증기→AI체크→2단계) + 3단계 자동 실행 — 폴더 감시 오케스트레이션 워처 | v1.0 | **버전 번호가 소스에 아예 없음** (docstring에 번호 자체가 없고, "v26 세션 추가" 같은 세션 단위 표기만 존재) — 07-08 버그 4건(이모지 인코딩/`input()` 대기/락 충돌/시작프로그램) 수정, 재부팅 테스트 통과. 위 검증기(v0.1) 호출이 이미 포함돼 있음 | v1.0 (07-07 최초 작성) | ⚠️ **"✅ 최신"에서 정정** — 실사용 수정 4건 + 검증기 연결이 문서에 전혀 반영 안 됨 |

> ⚠️ 위 표시가 있는 문서는 MD 내용이 실제 배포된 코드보다 오래된 상태다. 새
> 세션에서 이 문서들을 근거로 답할 때는 프로젝트 지식의 실제 코드 버전 기록도
> 함께 확인할 것.
>
> **🆕 콘솔 배너 지연 버그 (2026-07-08 소스 검토로 발견)**: 아래 5개 스크립트
> 모두, 파일 상단 docstring/변경이력은 최신인데 실행 시 화면에 찍히는 제목
> 줄(`print("...v버전")`)은 갱신되지 않았다. 사람이 "지금 실행되는 게 몇
> 버전이지?"를 화면만 보고 판단하면 틀린 답을 얻는다 — 반드시 파일 상단
> docstring을 봐야 한다. `풍림_발주자동화_워처.py`의 AI 체크(`ai_check()`)도
> 이 화면 출력을 그대로 읽고 판단하므로, 버전 자체를 근거로 삼는 판단에는
> (지금은 그런 로직이 없지만) 영향을 줄 수 있는 잠재 위험이다.
>
> | 스크립트 | 화면에 찍히는 버전 | 실제(docstring) 버전 | 격차 |
> |---|---|---|---|
> | `에브리데이발주.py` | v1.1 | v1.4 | 3단계 |
> | `발주서생성.py` | v2.5 | v2.7 | 2단계 |
> | `캐리어오더번호업데이트.py` | v1.3 | v1.4 | 1단계 |
> | `캐리어단가이력업로드.py` | v1.4 | v1.5 | 1단계 |
> | `매입처발주.py` | v1.10 | v1.10.1 | 1단계 |
> | `윈세이송장업데이트.py` | v2.0 | v2.0 | ✅ 유일하게 일치 |
>
> **🆕 05번 문서의 더 큰 문제 (버전 번호보다 심각) — 매칭 방식 자체가 바뀌었는데 문서는 옛날 방식을 설명함**:
> `05_윈세이스타리온_매입마감.md`은 윈세이송장업데이트.py를 "v1.1 — 사방넷
> 발주총괄을 재다운로드해서 사방넷주문번호로 매칭"이라고 설명하는데, 실제
> v2.0(소스 확인)은 이 방식을 완전히 버렸다. v2.0은 사방넷을 다시 받지 않고,
> 윈세이가 보내온 회신 파일을 전용 폴더(`3단계_발주원장업데이트\윈세이송장회신\`)
> 에서 직접 읽어 **연락처+모델코드**로 매칭한다(회신 파일엔 사방넷주문번호
> 자체가 없기 때문 — 캐리어오더번호업데이트.py와 같은 방식으로 전환).
> 버전 번호 하나만 다른 게 아니라 "무엇을 입력으로 쓰고 어떻게 매칭하는지"가
> 근본적으로 다르므로, 이 부분은 05번 문서에서 가장 먼저 새로 써야 하는
> 대목이다. (다행히 "송장번호 미입력 건만 대상, 매칭 성공=항상 최초 기록"
> 이라는 05 문서의 마감 판정 기준 자체는 v2.0에서도 그대로 유효함 — 소스로 확인.)
>
> **그 외 소스 검토로 발견한 사소한 불일치:**
> - `매입처발주.py`의 `CARRIER_COLS` 선언부 주석이 "캐리어 발주원장 컬럼
>   (24개)"라고 돼 있지만 실제 리스트는 31개다(주석만 안 고쳐짐).
> - `캐리어오더번호업데이트.py`의 docstring 제목은 "캐리어오더업데이트.py"로
>   적혀 있어 "번호" 글자가 빠져 있다(실제 파일명·워처의 참조 경로는 모두
>   "캐리어오더번호업데이트.py"라 동작엔 영향 없음, 표기 오타로 추정).
>
> **GitHub에 아직 백업 안 된 코드 (2026-07-08 기준):**
> `풍림_발주자동화_워처.py`(버전 미부여) · `캐리어_발주원장_검증.py` v0.1 ·
> `윈세이송장업데이트.py` v2.0 · `에브리데이발주.py` v1.4 · `발주서생성.py` v2.7 ·
> `캐리어단가이력업로드.py` v1.5 · `캐리어오더번호업데이트.py` v1.4 ·
> `캐리어발주원장_마감단가백필.py` v1.1(1회성, 정기 파이프라인 아님) ·
> `풍림_매입처관리 Code.gs` v13.2(GitHub 백업 여부 미확인 — poonglim-code에
> 있는지 직접 접근이 안 돼 확실친 않음, 매출관리포털의 별도 Code.gs와
> 혼동 주의) ·
> `발주서_관리_바로가기_만들기.ps1` · `시작프로그램_등록.ps1`

## 전체 흐름

```text
사방넷 발주총괄 다운로드 (수동)
        ↓
0단계 — 캐리어단가이력업로드.py   (매달 1회, 캐리어 운영안 → 단가테이블/이력)
        ↓
1단계 — 매입처발주.py             → 구글시트 캐리어/윈세이/스타리온_발주원장
        (병행) 에브리데이발주.py  → 캐리어_발주원장에 VRF 건 추가
        ↓
2단계 — 발주서생성.py             → 매입처별 발주서 생성 + (수동) 이메일 발송
        ↓
3단계 — 오더번호/송장 업데이트     → 구글시트 발주원장 완료여부 갱신
        ↓
ops.corcan.co.kr/operations/suppliers.html   ← 전체 현황 조회 및 수동 처리
```

**7단계(발주자동화워처)** 는 위 1→2단계, 3단계를 사람이 손으로 트리거하는
대신 자동으로 감시·실행·검증하는 오케스트레이션 레이어다. 01~03단계 스크립트
자체는 한 글자도 수정하지 않고 바깥에서 순서대로 호출한다 — 자세한 내용은
`07_발주자동화워처.md` 참조.

## 핵심 주의 — 자주 발생하는 문제

```text
단가가 틀려 보인다
→ 코드보다 먼저 캐리어_모델단가테이블.xlsx(또는 0단계가 만드는 단가/이력)의
  중복·결측을 의심한다. 2026-06-19 운영안 "특판건" 섹션 혼입 중복 사례 참조
  (02_매입처발주서생성.md 8-2번 항목).

캐리어_모델단가테이블.xlsx는 1단계와 2단계가 공유한다
→ 파일 하나만 교체하면 양쪽 모두 자동 정상화된다. 코드는 건드릴 필요 없다
  (01_매입처발주.md 4-4번, 02_매입처발주서생성.md 8-5번 항목).

윈세이/스타리온 단가가 틀려 보인다
→ 2단계는 윈세이/스타리온 단가를 다시 조회하지 않는다. 1단계(매입처발주.py)와
  해당 _모델단가테이블.xlsx 쪽을 먼저 확인한다
  (02_매입처발주서생성.md 9-4번, 10-3번 항목).

캐리어 발주가 안 됐는데 신규 카운트에 안 보인다
→ 신규주문/주문확인 상태는 1단계에서 해피콜대기로 분류되고, 2단계는
  완료여부=신규 인 건만 본다. 해피콜대기는 정상적으로 제외된 것이다
  (01_매입처발주.md 3-2번, 02_매입처발주서생성.md 10-4번 항목).

같은 사방넷주문번호에 행이 2개 있다 (기존재고 + 신규발주)
→ v1.10부터 도입된 부분재고 분할 결과로 정상이다. 처리구분/처리키 컬럼으로
  구분한다. suppliers.html v21부터 화면에서도 각 행을 독립적으로 처리한다
  (01_매입처발주.md 갱신 예정 항목, 06_풍림_매입처관리_AppsScript.md 참조).
```

## 재고 영향 있는 처리는 자동화하지 않는다

주문취소/취소재고 분기(오더번호 유무로 재고 복구 여부가 갈리는 처리)는 사람 확인 또는
명확한 기준으로만 처리한다. suppliers.html 취소처리 탭에서 안내 모달을 거쳐 처리한다.

## 구글시트 공통 정보

```text
이름: 풍림_매입처 관리시트
ID:   17VFCCy8UTr_UyWhnw1F1n0ZivY6G1XMeOv7VnkuQeRY
URL:  https://docs.google.com/spreadsheets/d/17VFCCy8UTr_UyWhnw1F1n0ZivY6G1XMeOv7VnkuQeRY
인증키: poonglim-0f980142b793.json
        (경로: Z:\풍림전자 사무 자동화\사무실 입고 및 AS\poonglim-0f980142b793.json — 공용)
```

## 관련 운영 화면

```text
ops.corcan.co.kr/operations/suppliers.html         매입처 발주 관리 현황판 (v21)
ops.corcan.co.kr/operations/suppliers-manual.html  매입처 발주 업무 매뉴얼
```

## 관련하지만 다른 문서 세트

```text
docs/sabang-converter/   거래처 발주서 → 사방넷 입력본 변환 (sabang.py). 이 문서 세트의
                         "앞단계"이며, 매입처(캐리어/윈세이/스타리온)와는 무관한 영역이다.
```
